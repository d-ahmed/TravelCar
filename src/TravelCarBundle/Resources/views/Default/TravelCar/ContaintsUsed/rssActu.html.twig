{% for item in items  %}
    <div class="col-md-12">
        <strong>{{ item.title }}</strong>
        <br>
        <small>{{ item.description }}</small>
        <br>
        <small><a href="{{ item.link }}">Voir +</a></small>
    </div>

{% endfor %}